# Source Assets

These files are retained for future editing and are not copied into the public site by Hugo.

- `brand/` contains the original high-resolution emblem supplied for Forge & Verse.
- `work/` contains original high-resolution work imagery before web compression.

Public, web-optimized assets live under `static/`.
